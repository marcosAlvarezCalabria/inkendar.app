# Availability draft recovery

Snapshot recovered from the landing repository on 2026-09-15 before cleaning misplaced, untracked software artifacts.

This directory is evidence and input for a future availability slice, not production-ready code. The `*-v2` files appear to be the latest variants; older files and patches are retained only to avoid losing potentially useful reasoning. Before integration, verify every contract against `docs/product/sellable-mvp-spec.md`, restore RED–GREEN–REFACTOR in the canonical package paths, run the full validation suite, and deliver through a separate pull request.

No customer data or live credentials are intentionally included. Test identifiers and token strings are synthetic fixtures.
